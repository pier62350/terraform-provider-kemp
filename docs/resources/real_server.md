---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kemp_real_server Resource - kemp"
subcategory: "Real Servers"
description: |-
  Manages a real server (backend) attached to a Kemp virtual service.
---

# kemp_real_server (Resource)

Manages a real server (backend) attached to a Kemp virtual service.

## Example Usage

```terraform
resource "kemp_real_server" "example" {
  virtual_service_id = kemp_virtual_service.example.id
  address            = "10.0.0.10"
  port               = "8080"
  weight             = 1000
  forward            = "nat"
  enable             = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `address` (String) IP address of the real server.
- `port` (String) Port on the real server.
- `virtual_service_id` (String) Index of the parent virtual service.

### Optional

- `critical` (Boolean) Optional. If true, the entire VS is marked down when this server fails. Default: `false`.
- `dns_name` (String) Optional. DNS hostname override for this real server. Empty string disables override.
- `enable` (Boolean) Optional. Whether the real server is enabled. Default: `true`.
- `follow` (Number) Optional. `RsIndex` of another real server to mirror health status from. Default: `0` (disabled).
- `forward` (String) Optional. Forwarding method: `nat` (default), `route`, or `transparent`.
- `limit` (Number) Optional. Maximum concurrent connections to this server. Default: `0` (unlimited).
- `weight` (Number) Optional. Load-balancing weight relative to other real servers in the pool. Default: `1000`.

### Read-Only

- `id` (Number) LoadMaster `RsIndex` of the real server.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import format: <virtual_service_index>/<real_server_index>
terraform import kemp_real_server.example 12/26
```
