---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kemp_config_user_certificate Resource - kemp"
subcategory: "LoadMaster Configuration"
description: |-
  Generates and manages a client certificate for a LoadMaster local user account. The certificate can be used for certificate-based admin UI authentication.
---

# kemp_config_user_certificate (Resource)

Generates and manages a client certificate for a LoadMaster local user account. The certificate can be used for certificate-based admin UI authentication.

## Example Usage

```terraform
# Generate a client certificate for an admin user
resource "kemp_config_user_certificate" "admin" {
  username   = "admin"
  passphrase = var.cert_passphrase
}

output "admin_certificate" {
  value     = kemp_config_user_certificate.admin.certificate
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `username` (String) **Required.** Username of the local account for which to generate the certificate. Forces replacement if changed.

### Optional

- `passphrase` (String, Sensitive) Optional. Passphrase to protect the generated private key. Write-only — not read back from the API. Forces replacement if changed.

### Read-Only

- `certificate` (String, Sensitive) Computed. PEM-encoded certificate and private key for the user. Populated after generation.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import kemp_config_user_certificate.admin admin
```
