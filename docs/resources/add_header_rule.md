---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kemp_add_header_rule Resource - kemp"
subcategory: ""
description: |-
  System-level rule that injects a request header. Attach to a virtual service via kemp_virtual_service_rule.
---

# kemp_add_header_rule (Resource)

System-level rule that injects a request header. Attach to a virtual service via `kemp_virtual_service_rule`.

## Example Usage

```terraform
resource "kemp_add_header_rule" "x_forwarded_proto" {
  name        = "x-forwarded-proto-https"
  header      = "X-Forwarded-Proto"
  replacement = "https"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `header` (String) Header field name to add.
- `name` (String) Unique rule name.
- `replacement` (String) Header value to set (max 255 chars).

### Optional

- `only_on_flag` (Number) Run only if this rule-chain flag (1-9) is set.
- `only_on_no_flag` (Number) Run only if this rule-chain flag (1-9) is NOT set.
