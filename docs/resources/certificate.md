---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kemp_certificate Resource - kemp"
subcategory: "Certificates"
description: |-
  Manages an SSL/TLS certificate stored on a Kemp LoadMaster (PEM or PFX). Cert content is immutable; changes force replacement.
---

# kemp_certificate (Resource)

Manages an SSL/TLS certificate stored on a Kemp LoadMaster (PEM or PFX). Cert content is immutable; changes force replacement.

## Example Usage

```terraform
resource "kemp_certificate" "example" {
  name = "wildcard-example-com"

  # Read a PEM bundle (cert + key concatenated) and base64-encode it.
  data = base64encode(file("${path.module}/wildcard-example-com.pem"))
}

# For a PFX with a passphrase:
#
# resource "kemp_certificate" "pfx" {
#   name     = "imported-pfx"
#   data     = base64encode(file("${path.module}/cert.pfx"))
#   password = var.pfx_password
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `data` (String, Sensitive) **Required.** Base64-encoded PFX bundle or PEM text (cert + key concatenated). Use `base64encode(file("path/to/cert.pem"))` in HCL. Forces replacement if changed.
- `name` (String) **Required.** Identifier under which LoadMaster stores the certificate. Forces replacement if changed.

### Optional

- `password` (String, Sensitive) Optional. Password for encrypted PFX bundles. Omit or leave empty for plain PEM. Forces replacement if changed.

### Read-Only

- `type` (String) Computed. Certificate type as reported by LoadMaster (e.g. `cert`, `pfx`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import an existing cert by its name on the LoadMaster.
terraform import kemp_certificate.example wildcard-example-com
```
