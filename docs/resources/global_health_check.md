---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kemp_global_health_check Resource - kemp"
subcategory: "LoadMaster Configuration"
description: |-
  Manages the global health check settings on a Kemp LoadMaster.
  This is a singleton resource — there is exactly one global health configuration per LoadMaster. Destroying this resource removes it from Terraform state but does not change the LoadMaster configuration.
  Note: The LoadMaster auto-computes retry_interval as retry_count * timeout + 1 when the other fields are modified. The provider always reads back the actual value after any write, so retry_interval may differ from what was specified.
---

# kemp_global_health_check (Resource)

Manages the global health check settings on a Kemp LoadMaster.

This is a singleton resource — there is exactly one global health configuration per LoadMaster. Destroying this resource removes it from Terraform state but does not change the LoadMaster configuration.

**Note:** The LoadMaster auto-computes `retry_interval` as `retry_count * timeout + 1` when the other fields are modified. The provider always reads back the actual value after any write, so `retry_interval` may differ from what was specified.

## Example Usage

```terraform
resource "kemp_global_health_check" "this" {
  timeout     = 5
  retry_count = 3
  # retry_interval is auto-computed by the LoadMaster as retry_count * timeout + 1
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `retry_count` (Number) Optional. Number of consecutive health check failures before marking a server down. Range: 1–15.
- `retry_interval` (Number) Optional. Global health check retry interval in seconds. Range: 9–120. Auto-computed by the LoadMaster as `retry_count * timeout + 1`.
- `timeout` (Number) Optional. Health check timeout in seconds. Range: 1–60.

### Read-Only

- `id` (String) Computed. Always `global`.
