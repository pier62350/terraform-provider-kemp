---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kemp_config_group Resource - kemp"
subcategory: "LoadMaster Configuration"
description: |-
  Manages a remote user group on a Kemp LoadMaster. Remote groups are used to map LDAP/Active Directory groups to LoadMaster permissions.
---

# kemp_config_group (Resource)

Manages a remote user group on a Kemp LoadMaster. Remote groups are used to map LDAP/Active Directory groups to LoadMaster permissions.

## Example Usage

```terraform
# Remote group with specific permissions
resource "kemp_config_group" "admins" {
  name        = "Domain Admins"
  permissions = ["real", "vs", "rules", "backup", "certs"]
}

# Read-only group
resource "kemp_config_group" "readonly" {
  name        = "Domain Users"
  permissions = []
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) **Required.** Name of the remote group. Forces replacement if changed.

### Optional

- `permissions` (List of String) Optional. List of permissions granted to the group. Valid values: `real`, `vs`, `rules`, `backup`, `certs`. An empty list means read-only access.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import kemp_config_group.admins "Domain Admins"
```
