# terraform-provider-kemp — codebase guide for contributors (human and Claude)

A Terraform provider for the **Progress Kemp LoadMaster** API v2. Built on
`terraform-plugin-framework`, hand-rolled HTTP client, MPL-2.0.

Published at `registry.terraform.io/providers/pier62350/kemp`.

---

## What's where

```
main.go                    Provider entry. Address: registry.terraform.io/pier62350/kemp.
internal/loadmaster/       Hand-rolled JSON-RPC client for Kemp's /accessv2 endpoint.
internal/provider/         Resource + data source implementations (terraform-plugin-framework).
examples/                  Per-resource HCL + import.sh used by tfplugindocs.
docs/                      Generated by tfplugindocs — do not hand-edit.
tools/                     Pins the tfplugindocs binary in its own go.mod.
.goreleaser.yml            Builds binaries for 10 OS/arch combos, signs SHA256SUMS.
.github/workflows/         test.yml on PR/push, release.yml on v* tags.
.claude/skills/            Reusable Claude Code skills (api-lookup, release).
terraform-registry-manifest.json  protocol_versions=["6.0"], required by the Registry.
```

## Resources at a glance

### Virtual Services

| Resource / Data Source | Status | Notes |
|---|---|---|
| `kemp_virtual_service` | ✅ | VS + SSL/SNI + ESP + WAF intercept_mode |
| `kemp_sub_virtual_service` | ✅ | Same surface as VS, created via `modvs createsubvs` |
| `kemp_real_server` | ✅ | Backend pool member |
| `kemp_virtual_service_rule` | ✅ | Attach a content rule to a VS by direction |
| `kemp_sub_virtual_service_rule` | ✅ | Attach a content rule to a SubVS by direction |
| `kemp_virtual_service_waf_rule` | ✅ | Attach a WAF rule (or rule set) to a VS; keys on VS triplet, not Index |
| `kemp_real_server_rule` | ✅ | Attach a content rule to an RS; import `<vs_id>/<rs_id>/<vs_port>/<vs_prot>/<rs_addr>/<rs_port>/<rule>` |
| `kemp_virtual_service_access_list_entry` | ✅ | Per-VS block/allow list entry; import `<vs_id>/<list_type>/<addr>` |

### Content Rules (system-level, rule types 0–5)

| Resource / Data Source | Status | Notes |
|---|---|---|
| `kemp_match_content_rule` | ✅ | Rule type 0 |
| `kemp_add_header_rule` | ✅ | Rule type 1 |
| `kemp_delete_header_rule` | ✅ | Rule type 2 |
| `kemp_replace_header_rule` | ✅ | Rule type 3 |
| `kemp_modify_url_rule` | ✅ | Rule type 4 |
| `kemp_replace_body_rule` | ✅ | Rule type 5 |

### Certificates & Security

| Resource / Data Source | Status | Notes |
|---|---|---|
| `kemp_certificate` | ✅ | PEM/PFX upload |
| `kemp_certificates` (data) | ✅ | List installed certificates |
| `kemp_acme_certificate` | ✅ | Let's Encrypt / DigiCert issuance |
| `kemp_acme_certificate_renewal` | ✅ | Force-renew an ACME cert |
| `kemp_acme_account` | ✅ | Service-level ACME bootstrap (one-shot) |
| `kemp_cipher_set` | ✅ | Custom TLS cipher sets |
| `kemp_sso_domain` | ✅ | SSO domain (Kerberos, SAML, NTLM, …) |

### WAF

| Resource / Data Source | Status | Notes |
|---|---|---|
| `kemp_owasp_custom_rule` / `…_data` | ✅ | OWASP/ModSecurity custom files (admin-level) |
| `kemp_waf_custom_rule` / `…_data` | ✅ | Legacy commercial WAF custom files |

### LoadMaster Configuration (`kemp_config_*`)

Global settings not tied to a specific VS or RS. All use the `kemp_config_` prefix.

| Resource / Data Source | Status | Notes |
|---|---|---|
| `kemp_config_global_health_check` | ✅ | Global default health-check parameters |
| `kemp_config_route` | ✅ | Static route |
| `kemp_config_hosts_entry` | ✅ | `/etc/hosts`-style local resolution entry |
| `kemp_config_interface` (data) | ✅ | Network interface details (read-only) |
| `kemp_config_ldap_endpoint` | ✅ | LDAP server for authentication |
| `kemp_config_local_user` | ✅ | Local WUI user account |
| `kemp_config_group` | ✅ | User permission group |
| `kemp_config_user_certificate` | ✅ | Per-user client certificate |
| `kemp_config_syslog` | ✅ | Syslog destination per severity level; import by level name e.g. `notice` |
| `kemp_config_ha` | ✅ | HA pair setup (mode, partner IP, shared IP, secret); import `loadmaster` |
| `kemp_config_packet_routing_filter` | ✅ | Packet routing filter (enabled, drop, restrict_to_interface, include_wui); import `loadmaster` |
| `kemp_config_intrusion_detection` | ✅ | IPS/IDS paranoia level 0–4; import `loadmaster` |
| `kemp_config_access_list_entry` | ✅ | Global block/allow list entry; import `<list_type>/<addr>` |
| `kemp_config_bandwidth_limit` | ✅ | Global client bandwidth limit per IP; import by address |
| `kemp_config_cps_limit` | ✅ | Global connections-per-second limit per IP; import by address |
| `kemp_config_rps_limit` | ✅ | Global requests-per-second limit per IP; import by address |
| `kemp_config_connection_limit` | ✅ | Global max concurrent connection limit per IP; import by address |
| `kemp_config_url_limit_rule` | ✅ | URL-based rate limiting rule (supports update); import by name |
| `kemp_config_interface_vlan` | ✅ | VLAN on a network interface; import `<interface_id>/<vlan_id>` |
| `kemp_config_interface_vxlan` | ✅ | VXLAN tunnel (supports update); import by interface_id |
| `kemp_config_interface_address` | ✅ | Additional IP address on an interface; import `<interface_id>/<address>` |
| `kemp_config_interface_bond` | ✅ | Bonded (LAG) interface with member management; import by interface_id |
| `kemp_config_cache_extension` | ✅ | Per-extension cache bypass; import by extension string |
| `kemp_config_compression_extension` | ✅ | Per-extension compression bypass; import by extension string |
| `kemp_config_network_telemetry` | ✅ | Network telemetry enable/disable per interface; import by interface |
| `kemp_config_geo` | ✅ | Enable/disable GEO load balancing globally; singleton, import `loadmaster` |
| `kemp_config_waf_update` | ✅ | WAF auto-update and install schedule; singleton, import `loadmaster` |
| `kemp_config_waf_logging` | ✅ | WAF remote logging (URI, credentials, format); singleton, import `loadmaster` |
| `kemp_config_kubernetes` | ✅ | Kubernetes ingress integration (mode, namespace, watch timeout); singleton, import `loadmaster` |

### GEO / Global Server Load Balancing (`kemp_gslb_*`)

| Resource / Data Source | Status | Notes |
|---|---|---|
| `kemp_gslb_cluster` | ✅ | GEO cluster (remote LoadMaster pool) |
| `kemp_gslb_location` | ✅ | Custom geographic location for IP range selection |
| `kemp_gslb_fqdn` | ✅ | FQDN with IP members and location weights |
| `kemp_gslb_params` | ✅ | DNS zone / SOA parameters for GSLB; singleton, import `loadmaster` |
| `kemp_gslb_ip_range` | ✅ | IP range to location mapping; import `<start_ip>/<end_ip>` |
| `kemp_gslb_acl_entry` | ✅ | GEO ACL custom entry (allow/block by CIDR); import `<type>/<cidr>` |

**Every resource implements `ImportState`.** Match the pattern of an existing
resource when adding new ones.

---

## Wire-format quirks — important, do not unlearn

These come from the official spec at https://loadmasterapiv2.docs.progress.com/
and from validation against a live LoadMaster. **The spec is the source of
truth.** Notes on patterns that aren't obvious:

1. **VS reference: bare numeric Index.** `vs: "12"` works for `showvs/modvs/delvs`.
   `vs: "!12"` fails — Kemp parses `!12` as an address and complains `port:
   String Value missing`.

2. **RS reference: bang-prefixed Index.** `rs: "!5"` for showrs/modrs/delrs.
   Without the bang, Kemp tries to look up by address.

3. **`addrs` is the odd one out.** It uses `vs` (bare numeric, *not* bang),
   `rs` (the RS *address*, not Index), and `rsport` (not `port`).

4. **`vsaddwafrule` keys on the VS triplet** (`vs`+`port`+`prot`), not the
   Index. That's why `kemp_virtual_service_waf_rule` takes
   `virtual_service_address/port/protocol` instead of an `id`.

5. **SubVS creation goes through the parent's `modvs`** with
   `createsubvs: ""`. There's no `addsubvs` command. The response includes
   the new SubVS's Index, used for all subsequent CRUD.

6. **PascalCase response fields.** Kemp returns JSON with capitalized keys
   (`Index`, `VSAddress`, `RsIndex`). Use explicit `json:"PascalName"` tags
   on Go structs — don't rely on automatic snake_case conversion.

7. **`status: "fail"` at HTTP 200.** Even on a successful HTTP response,
   Kemp may return `{"code":422,"status":"fail","message":"..."}`. The
   internal `loadmaster.Client.call` already checks for this and surfaces
   `*loadmaster.Error` accordingly.

8. **CertFile is comma-separated** — multiple cert names mean SNI. The
   provider exposes this as a `cert_files` list in HCL and joins on send.

---

## Workflows extracted to skills

To keep this file scannable, two procedural workflows live as Claude Code
skills under `.claude/skills/`:

- **`kemp-api-lookup`** — query the official Postman collection for a
  specific `/accessv2` command's example body, parameter docs, and
  response shape. Invoke before implementing or extending any resource
  that calls a new Kemp command. Falls back to refreshing the cached
  spec from the upstream Postman URL.

- **`kemp-release`** — preflight (clean tree, tests, vet, regenerated
  docs), tag a semver `vX.Y.Z`, push, watch the `Release` workflow,
  verify the 16 expected GitHub-release assets, troubleshoot common
  failures (GPG import, goreleaser deprecations, signature mismatch).

The skills are committed under `.claude/skills/`, so they're available
to anyone working in this repo with Claude Code.

---

## Local dev workflow

```bash
make build        # go build -v ./...
make install      # go install -v ./...   → ~/go/bin/terraform-provider-kemp
make test         # unit tests
make generate     # cd tools; go generate ./...   → regenerates docs/
make testacc      # acceptance tests; needs KEMP_HOST + KEMP_API_KEY (or user/pass)
```

Use `dev_overrides` in `~/.terraformrc` to test against a local build
without `terraform init`:

```hcl
provider_installation {
  dev_overrides {
    "pier62350/kemp" = "/home/<user>/go/bin"
  }
  direct {}
}
```

Acceptance tests **must not** target addresses already in use on the
target LoadMaster. Use a dedicated lab unit with fresh VS addresses.
Examples in this repo use `10.0.x.x` for that reason.

---

## Conventions to follow when contributing

- **Always implement `ImportState`** on new resources. Use the simplest
  sensible ID format: a single name for resources keyed by name, a slash-
  separated tuple for nested ones (`<parent>/<child>` for real_server,
  sub_virtual_service; `<vs>/<direction>/<rule>` for VS↔rule attachment).

- **Go struct field tags**: explicit `json:"PascalName"` on every field.
  The wire format is fixed by Kemp — don't rely on automatic naming.

- **Don't introduce retry/backoff libraries.** A previous scaffold
  inherited `cenkalti/backoff/v5` from a reference repo; it was overkill
  for LAN calls. If transient failures become real, add a 10-line stdlib
  retry, not a dependency.

- **Don't depend on third-party Kemp client libraries.** The
  `internal/loadmaster` package is hand-rolled MPL-2.0 because the
  reference implementations available online have no LICENSE.

- **No emojis, no AI co-author trailers in commits.** Standard hygiene.

- **MarkdownDescription drives the rendered Registry docs.** Keep field
  descriptions accurate, terse, and example-rich; they appear verbatim in
  `docs/<kind>/<name>.md` after `make generate`.

---

## Roadmap (loose, in priority order)

- **Run live acceptance tests** — Tests exist for VS, SubVS, SubVS rule,
  and real server in `internal/provider/*_test.go`. Run against a dedicated
  lab unit (not 192.168.1.55 / 192.168.1.251) with VS addresses in the
  10.0.0.100–10.0.0.103 range:
  ```
  TF_ACC=1 KEMP_HOST=https://192.168.1.155 KEMP_API_KEY=... make testacc
  ```

- **GEO / GSLB** — `kemp_gslb_fqdn`, `kemp_gslb_cluster`, `kemp_gslb_location`; requires GEO license.
